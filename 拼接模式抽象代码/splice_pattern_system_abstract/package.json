{
  "name": "splice_pattern_system_abstract",
  "version": "0.0.1",
  "scripts": {
    "watch": "tsc -w -noEmit",
    "preprocess:ts": "gulp createMergedTargetChunkFile_ts",
    "preprocess:res": "gulp createMergedTargetChunkFile_res"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "typescript": "^4.2.3"
  }
}